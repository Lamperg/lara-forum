#  Docker-template for MacOS development

### Requirements

**Docker, Docker-compose and Docker-sync**

```
  brew tap caskroom/cask
  brew cask install docker
  brew install docker-compose
  sudo gem install docker-sync
```

### License

MIT © 2019
