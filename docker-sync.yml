version: "2"

options:
  verbose: true

syncs:

  test-app-sync:
    src: './'
    sync_host_port: 10877
    sync_userid: '33'
    sync_strategy: 'native_osx'
    host_disk_mount_mode: 'cached'
    sync_excludes: [".idea", ".git"]
